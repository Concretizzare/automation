# Ralph Configuration for Claude Code Subscription Users
# Copy this file to .env if you use Claude Code with an active subscription

# ============================================
# Execution Mode
# ============================================

# Use Claude Code directly (no Docker needed)
RALPH_USE_DOCKER=no

# ============================================
# Notifications (macOS)
# ============================================

# Enable desktop notifications
RALPH_NOTIFY_TOOL=terminal-notifier

# ============================================
# Performance
# ============================================

# Seconds between iterations
RALPH_COOLING_PERIOD=2

# Max iterations (can override via command line: ./ralph.sh 20)
RALPH_MAX_ITERATIONS=10

# ============================================
# Notes for Subscription Users
# ============================================

# ✅ NO ANTHROPIC_API_KEY NEEDED
# Claude Code subscription handles authentication automatically
# The 'claude' command uses your logged-in session

# ✅ Docker is optional
# Set RALPH_USE_DOCKER=no to use Claude CLI directly
# This is faster and simpler for local development

# ✅ Command line takes precedence
# ./ralph.sh 20  will use 20 iterations regardless of MAX_ITERATIONS env var
