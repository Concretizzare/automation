# Ralph Configuration Environment Variables
# Copy this file to .env in the ralph-hybrid root directory and customize

# ============================================
# Execution Mode
# ============================================

# Docker usage: auto (detect), yes (force Docker), no (force CLI)
# Default: auto
# RALPH_USE_DOCKER=auto

# Docker image name (only used if Docker mode enabled)
# Default: claude-code
# RALPH_DOCKER_IMAGE=claude-code

# Docker sandbox permission mode
# Options: acceptEdits, manual, readonly
# Default: acceptEdits
# RALPH_DOCKER_PERMISSION=acceptEdits

# ============================================
# CLI Mode Settings
# ============================================

# Claude CLI permission flags
# Default: --dangerously-skip-permissions
# WARNING: Only use in trusted environments
# RALPH_CLAUDE_PERMISSION=--dangerously-skip-permissions

# Alternative (manual approval for each action):
# RALPH_CLAUDE_PERMISSION=

# ============================================
# Notifications
# ============================================

# Notification tool: auto (detect), tt, terminal-notifier, notify-send, none
# Default: auto
# RALPH_NOTIFY_TOOL=auto

# Force specific tool:
# RALPH_NOTIFY_TOOL=terminal-notifier  # macOS
# RALPH_NOTIFY_TOOL=notify-send        # Linux
# RALPH_NOTIFY_TOOL=tt                 # Cross-platform

# Disable notifications:
# RALPH_NOTIFY_TOOL=none

# ============================================
# Performance Tuning
# ============================================

# Seconds to wait between iterations (prevents rate limiting)
# Default: 2
# Both RALPH_COOLING_PERIOD and COOLING_PERIOD are supported (RALPH_* takes precedence)
# COOLING_PERIOD=2
# RALPH_COOLING_PERIOD=2

# Increase for rate-limited API:
# COOLING_PERIOD=5

# Maximum iterations
# Default: 10
# Both RALPH_MAX_ITERATIONS and MAX_ITERATIONS are supported (RALPH_* takes precedence)
# Note: Command line argument (./ralph.sh 20) takes precedence over environment variables
# MAX_ITERATIONS=10
# RALPH_MAX_ITERATIONS=10

# ============================================
# Debugging (TODO: Not yet implemented in ralph.sh)
# ============================================

# TODO: Not yet implemented - Enable verbose logging
# DEBUG=1

# TODO: Not yet implemented - Keep temporary prompt files for inspection
# KEEP_TEMP_FILES=1

# ============================================
# Claude API Settings
# ============================================

# Anthropic API key (required for Claude CLI)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# ============================================
# Project-Specific Settings (TODO: Not yet implemented in ralph.sh)
# ============================================

# TODO: Not yet implemented - Default branch to create feature branches from
# DEFAULT_BASE_BRANCH=main

# Note: Git author settings are standard Git environment variables (not Ralph-specific)
# GIT_AUTHOR_NAME="Your Name"
# GIT_AUTHOR_EMAIL="your.email@example.com"

# ============================================
# CI/CD Settings
# ============================================

# When running in CI, you might want:
# RALPH_USE_DOCKER=yes           # Ensure Docker mode
# RALPH_NOTIFY_TOOL=none         # Disable notifications
# MAX_ITERATIONS=20              # Allow more iterations
# COOLING_PERIOD=1               # Faster iteration (if API allows)

# ============================================
# Examples for Different Environments
# ============================================

# Development (macOS with Docker Desktop):
# RALPH_USE_DOCKER=yes
# RALPH_NOTIFY_TOOL=terminal-notifier

# Development (Linux):
# RALPH_USE_DOCKER=yes
# RALPH_NOTIFY_TOOL=notify-send

# Development (no Docker, using CLI):
# RALPH_USE_DOCKER=no
# RALPH_NOTIFY_TOOL=auto

# CI/CD (GitHub Actions):
# RALPH_USE_DOCKER=yes
# RALPH_NOTIFY_TOOL=none
# MAX_ITERATIONS=50
# COOLING_PERIOD=0

# ============================================
# Notes
# ============================================

# - Environment variables override script defaults
# - Command-line arguments override environment variables
# - Commented values show defaults (you don't need to set them unless changing)
# - This file should NOT be committed to git (add to .gitignore)
